$("#cox").flip({
  axis: 'y',
  trigger: 'hover'
});

$("#att").flip({
  axis: 'y',
  trigger: 'hover'
});

$("#dish").flip({
  axis: 'y',
  trigger: 'hover'
});

$("#gator").flip({
  axis: 'y',
  trigger: 'hover'
});


$("#hotspots").flip({
  axis: 'y',
  trigger: 'hover'
});

$("#windstream").flip({
  axis: 'y',
  trigger: 'hover'
});
